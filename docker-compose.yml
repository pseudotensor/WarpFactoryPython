version: '3.8'

services:
  warpfactory-dev:
    build: .
    container_name: warpfactory_conversion
    volumes:
      - .:/warpfactory_py
      - ..:/WarpFactory
      - ../.claude_docker1:/home/warpuser/.claude
      - /opt/matlab:/opt/matlab:ro
      - /home/jon/.matlab/R2023b_licenses:/home/warpuser/.matlab/R2023b_licenses:ro
      - matlab_prefs:/home/warpuser/.matlab/R2023b
    working_dir: /warpfactory_py
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
      - JUPYTER_ENABLE_LAB=yes
      - INIT_ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: /bin/bash

volumes:
  matlab_prefs:
